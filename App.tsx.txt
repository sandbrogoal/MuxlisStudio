/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 */

import React, { useState } from "react";
import { motion, AnimatePresence } from "motion/react";
import { 
  Instagram, 
  Sparkles, 
  Video, 
  Users, 
  Target, 
  Zap, 
  ChevronRight,
  Star,
  CheckCircle2,
  Clock,
  Crown,
  Rocket,
  Youtube,
  Mic
} from "lucide-react";

interface Service {
  icon: React.ReactNode;
  title: string;
  desc: string;
}

interface Package {
  id: string;
  name: string;
  price: string;
  description: string;
  icon: React.ReactNode;
  services: Service[];
}

const packages: Package[] = [
  {
    id: "start",
    name: "Start",
    price: "500",
    description: "Kichik bizneslar va startaplar uchun ideal boshlanish",
    icon: <Rocket className="w-4 h-4" />,
    services: [
      {
        icon: <Instagram className="w-5 h-5 text-gold" />,
        title: "Instagram upakovka",
        desc: "Profilni professional brend holatiga keltirish"
      },
      {
        icon: <Target className="w-5 h-5 text-gold" />,
        title: "SMM Strategiya",
        desc: "Mukammal rejalashtirish va kreativ yondashuv"
      },
      {
        icon: <Sparkles className="w-5 h-5 text-gold" />,
        title: "5 ta AI Video",
        desc: "Sun'iy intellekt yordamida yaratilgan kreativ videolar"
      },
      {
        icon: <Video className="w-5 h-5 text-gold" />,
        title: "5 ta Jonli Reels",
        desc: "Brend egasi ishtirokidagi professional Reelslar"
      },
      {
        icon: <Zap className="w-5 h-5 text-gold" />,
        title: "Target Video",
        desc: "Sotuvga yo'naltirilgan 1 ta maxsus target video"
      },
      {
        icon: <Clock className="w-5 h-5 text-gold" />,
        title: "1 oylik qo'llab-quvvatlash",
        desc: "Doimiy aloqa va natijalar monitoringi"
      }
    ]
  },
  {
    id: "pro",
    name: "Pro",
    price: "800",
    description: "Professional brending va rivojlangan SMM strategiya",
    icon: <Star className="w-4 h-4" />,
    services: [
      {
        icon: <Instagram className="w-5 h-5 text-gold" />,
        title: "Instagram upakovka",
        desc: "Profilni ochish va professional brend holatiga keltirish"
      },
      {
        icon: <Target className="w-5 h-5 text-gold" />,
        title: "Brend Ustida Ishlash",
        desc: "Ranglar, shriftlar va professional dizayn tizimi"
      },
      {
        icon: <Sparkles className="w-5 h-5 text-gold" />,
        title: "5 ta AI Video",
        desc: "Sun'iy intellekt yordamida yaratilgan kreativ videolar"
      },
      {
        icon: <Users className="w-5 h-5 text-gold" />,
        title: "5 ta Ma'lumotli Post",
        desc: "Mahsulot haqida to'liq va foydali ma'lumotlar"
      },
      {
        icon: <Video className="w-5 h-5 text-gold" />,
        title: "5 ta Jonli Reels",
        desc: "Brend egasi ishtirokidagi professional Reelslar"
      },
      {
        icon: <Zap className="w-5 h-5 text-gold" />,
        title: "1 ta Target Video",
        desc: "$100 xarajatni ichiga oluvchi professional video"
      },
      {
        icon: <Sparkles className="w-5 h-5 text-gold" />,
        title: "Har kuni 3-4 ta Story",
        desc: "Doimiy aktivlik va mijozlar bilan muloqot"
      }
    ]
  },
  {
    id: "vip",
    name: "VIP",
    price: "1500",
    description: "YouTube prodakshn, Podcast va Muxlisa Po'latovadan shaxsiy strategik tahlil",
    icon: <Crown className="w-4 h-4" />,
    services: [
      {
        icon: <Instagram className="w-5 h-5 text-gold" />,
        title: "Instagram upakovka",
        desc: "Brend ustida kuchli ishlash (rang, shrift, dizayn)"
      },
      {
        icon: <Youtube className="w-5 h-5 text-gold" />,
        title: "YouTube upakovka",
        desc: "Kanalni ochish va professional brending"
      },
      {
        icon: <Mic className="w-5 h-5 text-gold" />,
        title: "1 soatlik Podcast",
        desc: "Professional darajadagi 1 ta kontent podcast"
      },
      {
        icon: <Sparkles className="w-5 h-5 text-gold" />,
        title: "7 ta AI Video",
        desc: "Trenddagi eng so'nggi AI videolar"
      },
      {
        icon: <Users className="w-5 h-5 text-gold" />,
        title: "7 ta Ma'lumotli Post",
        desc: "Mahsulot haqida mukammal tushuntirishlar"
      },
      {
        icon: <Video className="w-5 h-5 text-gold" />,
        title: "10 ta Jonli Reels",
        desc: "Brend egasi bilan doimiy va sifatli Reelslar"
      },
      {
        icon: <Zap className="w-5 h-5 text-gold" />,
        title: "2 ta Target Video",
        desc: "$150 xarajatni ichiga oluvchi reklama videolari"
      },
      {
        icon: <Star className="w-5 h-5 text-gold" />,
        title: "Muxlisa Po'latova",
        desc: "Har 15 kunda brendni tahlil qilish va strategik maslahatlar"
      }
    ]
  }
];

const team = [
  { role: "Muxlisa Po'latova", icon: <Star className="w-4 h-4" /> },
  { role: "AI xodim", icon: <Sparkles className="w-4 h-4" /> },
  { role: "SMM xodim", icon: <Users className="w-4 h-4" /> },
  { role: "Targetolog", icon: <Target className="w-4 h-4" /> },
  { role: "Storiesmeyker", icon: <Instagram className="w-4 h-4" /> }
];

const containerVariants = {
  hidden: { opacity: 0 },
  visible: {
    opacity: 1,
    transition: {
      staggerChildren: 0.1
    }
  }
};

const itemVariants = {
  hidden: { opacity: 0, y: 10 },
  visible: { opacity: 1, y: 0 }
};

export default function App() {
  const [activeTab, setActiveTab] = useState(packages[0].id);
  const currentPackage = packages.find(p => p.id === activeTab) || packages[0];

  return (
    <div className="min-h-screen bg-noir flex flex-col items-center selection:bg-gold/30 pb-32">
      {/* 9:16 Aspect Ratio Container for Mobile Feel */}
      <div className="w-full max-w-[440px] px-6 py-12 md:py-16 flex flex-col gap-10">
        
        {/* Header Section */}
        <motion.header 
          initial={{ opacity: 0, y: -20 }}
          animate={{ opacity: 1, y: 0 }}
          className="text-center flex flex-col items-center gap-4"
        >
          <div className="inline-block px-3 py-1 border border-white/20 rounded-full text-[10px] uppercase tracking-[0.2em] font-semibold text-white/60">
            Muxlis Studio Presents
          </div>
          <h1 className="font-serif text-5xl md:text-6xl font-light italic tracking-tight">
            Muxlis <span className="gold-text not-italic font-bold">Studio</span>
          </h1>
          <p className="text-white/40 text-sm tracking-widest uppercase italic">Digital Agency Services</p>
        </motion.header>

        {/* Tab Switcher */}
        <div className="flex bg-white/[0.03] p-1.5 rounded-2xl border border-white/5 shadow-2xl">
          {packages.map((pkg) => (
            <button
              key={pkg.id}
              onClick={() => setActiveTab(pkg.id)}
              className={`flex-1 flex items-center justify-center gap-2 py-3 rounded-xl text-xs font-semibold uppercase tracking-wider transition-all relative ${
                activeTab === pkg.id ? "text-noir" : "text-white/40 hover:text-white/70"
              }`}
            >
              {activeTab === pkg.id && (
                <motion.div 
                  layoutId="activeTab"
                  className="absolute inset-0 bg-white rounded-xl"
                  transition={{ type: "spring", bounce: 0.2, duration: 0.6 }}
                />
              )}
              <span className="relative z-10 flex items-center gap-2">
                {pkg.icon}
                {pkg.name}
              </span>
            </button>
          ))}
        </div>

        {/* Animated Content Wrapper */}
        <AnimatePresence mode="wait">
          <motion.div
            key={activeTab}
            initial={{ opacity: 0, x: 20 }}
            animate={{ opacity: 1, x: 0 }}
            exit={{ opacity: 0, x: -20 }}
            transition={{ duration: 0.3 }}
            className="flex flex-col gap-10"
          >
            {/* Pricing Card */}
            <div className="relative group">
              <div className="absolute -inset-0.5 bg-gradient-to-r from-gold to-bronze rounded-3xl blur opacity-20 group-hover:opacity-40 transition duration-1000"></div>
              <div className="relative premium-gradient rounded-3xl p-8 border border-white/10 flex flex-col items-center gap-6 text-center shadow-2xl">
                <div className="flex items-center gap-2 text-gold">
                  <Star className="w-4 h-4 fill-gold" />
                  <span className="uppercase text-[11px] font-bold tracking-[0.3em]">{currentPackage.name} Tarifi</span>
                  <Star className="w-4 h-4 fill-gold" />
                </div>
                
                <div className="flex flex-col gap-1">
                  <div className="text-6xl font-serif font-bold italic">${currentPackage.price}</div>
                  <div className="text-white/40 uppercase text-[10px] tracking-widest font-medium">Barchasi kiritilgan</div>
                </div>

                <div className="h-px w-full bg-gradient-to-r from-transparent via-white/10 to-transparent"></div>

                <p className="text-xs text-white/50 leading-relaxed max-w-[280px]">
                  {currentPackage.description}
                </p>
              </div>
            </div>

            {/* Services List */}
            <motion.section 
              variants={containerVariants}
              initial="hidden"
              animate="visible"
              className="flex flex-col gap-3"
            >
              <h2 className="text-[10px] uppercase tracking-[0.4em] text-white/20 font-bold mb-1 ml-1 leading-none">Xizmatlar ro'yxati</h2>
              {currentPackage.services.map((service, index) => (
                <motion.div 
                  key={index}
                  variants={itemVariants}
                  className="group flex gap-4 items-center p-3.5 rounded-2xl border border-white/5 bg-white/[0.02] hover:bg-white/[0.04] transition-all hover:translate-x-1"
                >
                  <div className="p-2.5 rounded-xl bg-white/[0.03] border border-white/10 group-hover:border-gold/30 transition-colors shrink-0">
                    {service.icon}
                  </div>
                  <div className="flex flex-col gap-0.5">
                    <h3 className="font-semibold text-sm tracking-wide text-white/90">{service.title}</h3>
                    <p className="text-[11px] text-white/30 leading-snug">{service.desc}</p>
                  </div>
                </motion.div>
              ))}
            </motion.section>
          </motion.div>
        </AnimatePresence>

        {/* Team Section */}
        <motion.section 
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 0.5 }}
          className="flex flex-col gap-6"
        >
          <div className="flex flex-col items-center gap-4">
            <h2 className="text-[10px] uppercase tracking-[0.4em] text-white/20 font-bold mb-w leading-none">Siz bilan ishlaydigan jamoa</h2>
            <div className="flex flex-wrap justify-center gap-2.5">
              {team.map((member) => (
                <div key={member.role} className="flex items-center gap-2 px-4 py-2 bg-white/[0.03] border border-white/5 rounded-full hover:bg-white/[0.06] transition-colors cursor-default">
                  <div className="text-gold/60">{member.icon}</div>
                  <span className="text-[10px] font-semibold text-white/50 uppercase tracking-widest">{member.role}</span>
                </div>
              ))}
            </div>
          </div>
        </motion.section>

        {/* CTA Footer */}
        <motion.div 
          initial={{ y: 20, opacity: 0 }}
          animate={{ y: 0, opacity: 1 }}
          className="fixed bottom-6 w-full max-w-[440px] px-6 left-1/2 -translate-x-1/2 z-50 pt-4"
        >
          <button 
            className="w-full relative group shadow-[0_20px_50px_rgba(0,0,0,0.5)]"
            onClick={() => window.open("https://t.me/MuxlisaPulatova", "_blank")}
          >
            <div className="absolute -inset-0.5 bg-gradient-to-r from-gold to-bronze rounded-2xl blur opacity-30 group-hover:opacity-100 transition duration-500"></div>
            <div className="relative bg-white text-black font-bold h-16 rounded-2xl flex items-center justify-center gap-3 overflow-hidden group-active:scale-95 transition-transform">
              <span className="uppercase tracking-[0.2em] text-xs">Ushbu tarifni band qilish</span>
              <ChevronRight className="w-5 h-5 group-hover:translate-x-1 transition-transform" />
              
              {/* Shine effect */}
              <div className="absolute inset-x-0 top-0 h-1/2 bg-gradient-to-b from-white/20 to-transparent pointer-events-none"></div>
            </div>
          </button>
          
          <div className="flex justify-center items-center gap-2 mt-4 bg-noir/50 backdrop-blur-sm py-1 rounded-full">
            <CheckCircle2 className="w-3 h-3 text-gold" />
            <span className="text-[9px] text-white/20 uppercase tracking-[0.2em] font-bold">100% Sifat va Natija Kafolatlangan</span>
          </div>
        </motion.div>

      </div>

      {/* Decorative Elements */}
      <div className="fixed inset-0 pointer-events-none overflow-hidden -z-10">
        <div className="absolute top-[-10%] left-[-10%] w-[50%] h-[50%] bg-gold/5 blur-[150px] rounded-full"></div>
        <div className="absolute bottom-[-10%] right-[-10%] w-[50%] h-[50%] bg-bronze/5 blur-[150px] rounded-full"></div>
      </div>
    </div>
  );
}
